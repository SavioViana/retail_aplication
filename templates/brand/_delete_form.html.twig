<form method="post" action="{{ path('brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Você tem certeza que quer remover este item?');" class="float-right">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
